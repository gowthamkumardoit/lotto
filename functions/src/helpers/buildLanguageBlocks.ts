import { formatSettledResult } from "./formatSettledResult";

export function buildLanguageBlocks(after: any) {
  switch (after.status) {
    case "OPEN":
      return {
        en:
          `🔥🎯 ${after.name} is LIVE!\n` +
          `⏳ Limited time — book your tickets NOW!\n` +
          `💰 Big prize waiting!`,
        ta:
          `🔥🎯 ${after.name} தொடங்கிவிட்டது!\n` +
          `⏳ நேரம் குறைவு — இப்போதே டிக்கெட் வாங்குங்கள்!\n` +
          `💰 பெரிய பரிசு காத்திருக்கிறது!`,
        ml:
          `🔥🎯 ${after.name} ആരംഭിച്ചു!\n` +
          `⏳ സമയം കുറവാണ് — ഇപ്പോൾ ടിക്കറ്റ് വാങ്ങൂ!`,
        hi:
          `🔥🎯 ${after.name} शुरू हो गया है!\n` +
          `⏳ समय सीमित है — अभी टिकट बुक करें!`,
      };

    case "LOCKED":
      return {
        en: `🔒⛔ ${after.name} is CLOSED!\nResults coming soon…`,
        ta: `🔒⛔ ${after.name} முடிவடைந்தது!\nமுடிவுகள் விரைவில்…`,
        ml: `🔒⛔ ${after.name} അടച്ചിരിക്കുന്നു!\nഫലങ്ങൾ ഉടൻ…`,
        hi: `🔒⛔ ${after.name} बंद हो गया है!\nपरिणाम जल्द आएंगे…`,
      };

    case "DRAWN":
      return {
        en: `🎰✨ ${after.name} draw completed!`,
        ta: `🎰✨ ${after.name} டிரா முடிந்தது!`,
        ml: `🎰✨ ${after.name} ഡ്രോ പൂർത്തിയായി!`,
        hi: `🎰✨ ${after.name} ड्रॉ पूरा हुआ!`,
      };

    case "SETTLED":
      const resultText = formatSettledResult(after.settledResult);
      return {
        en: `🏆💥 ${after.name} RESULTS ARE OUT!\n\n${resultText}`,
        ta: `🏆💥 ${after.name} முடிவுகள் வெளியானது!\n\n${resultText}`,
        ml: `🏆💥 ${after.name} ഫലങ്ങൾ പ്രഖ്യാപിച്ചു!\n\n${resultText}`,
        hi: `🏆💥 ${after.name} परिणाम घोषित!\n\n${resultText}`,
      };

    default:
      return null;
  }
}
